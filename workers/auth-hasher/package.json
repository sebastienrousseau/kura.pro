{
  "name": "cloudcdn-auth-hasher",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Worker that runs Argon2id password hashing/verification for the cloudcdn-pro Pages project. Called only via cross-script service binding — no public HTTP surface.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "hash-wasm": "^4.11.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

# common — shared image library

**Not a tenant zone.** Common stock imagery (photography, illustrations) reused across multiple tenants. The companion to `cmn/` for image-heavy assets.

Routing: `https://cloudcdn.pro/common/images/...` resolves to `clients/common/images/...`. Treat it as a globally shared library — additions should be coordinated rather than tenant-local.

{
  "name": "kura-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local image conversion scripts for kura.pro CDN",
  "scripts": {
    "test": "npm test --prefix ..",
    "test:watch": "npm run test:watch --prefix .."
  },
  "dependencies": {
    "sharp": "0.33.5"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1"
  }
}
